local Rayfield = loadstring(game:HttpGet("https://raw.githubusercontent.com/dano0011/orion-moded/refs/heads/main/rayfield"))()
loadstring(game:HttpGet("https://raw.githubusercontent.com/dkhub43221/label/refs/heads/main/Bypass.toolz"))()
local executor = identifyexecutor and identifyexecutor() or "Unknown"

local customTheme = {
    TextColor = Color3.fromRGB(245, 255, 245),
    Background = Color3.fromRGB(17, 16, 20),
    Topbar = Color3.fromRGB(17, 16, 20),
    Shadow = Color3.fromRGB(0, 0, 0),

    NotificationBackground = Color3.fromRGB(17, 16, 20),
    NotificationTextColor = Color3.fromRGB(245, 255, 245),
    NotificationActionsBackground = Color3.fromRGB(15, 55, 25),

    TabBackground = Color3.fromRGB(10, 14, 11),
    TabStroke = Color3.fromRGB(18, 35, 22),
    TabBackgroundSelected = Color3.fromRGB(18, 38, 23),
    TabTextColor = Color3.fromRGB(150, 165, 150),
    SelectedTabTextColor = Color3.fromRGB(120, 255, 110),

    ElementBackground = Color3.fromRGB(17, 16, 20),
    ElementBackgroundHover = Color3.fromRGB(18, 28, 20),
    SecondaryElementBackground = Color3.fromRGB(10, 13, 11),

    ElementStroke = Color3.fromRGB(35, 95, 45),
    SecondaryElementStroke = Color3.fromRGB(17, 16, 20),

    SliderBackground = Color3.fromRGB(30, 40, 32),
    SliderProgress = Color3.fromRGB(77, 251, 16),
    SliderStroke = Color3.fromRGB(77, 251, 16),

    ToggleBackground = Color3.fromRGB(55, 60, 57),
    ToggleEnabled = Color3.fromRGB(77, 251, 16),
    ToggleDisabled = Color3.fromRGB(55, 60, 57),
    ToggleEnabledStroke = Color3.fromRGB(90, 95, 90),
    ToggleDisabledStroke = Color3.fromRGB(90, 95, 90),
    ToggleEnabledOuterStroke = Color3.fromRGB(90, 95, 90),
    ToggleDisabledOuterStroke = Color3.fromRGB(90, 95, 90),

    DropdownSelected = Color3.fromRGB(18, 38, 23),
    DropdownUnselected = Color3.fromRGB(12, 15, 13),

    InputBackground = Color3.fromRGB(12, 15, 13),
    InputStroke = Color3.fromRGB(77, 251, 16),
}

local player = Players.LocalPlayer
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local ProximityPromptService = game:GetService("ProximityPromptService")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local Backpack = LocalPlayer:WaitForChild("Backpack")


local Window = Rayfield:CreateWindow({
    Name = "《 🍀 DK's HUB 》 | Cali Streets | V0.0 | " .. executor,
    ScriptID = "sid_28nk80xky15u",
    ShowText = "DK's HUB",
    Icon = 0,
    Theme = customTheme,
    DisableRayfieldPrompts = true,
    DisableBuildWarnings = true,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "Dks Hub",
        FileName = ""
    },
    Discord = {
        Enabled = true,
        Invite = "discord.gg/xdkhub",
        RememberJoins = true
    },
    KeySystem = false,
    KeySettings = {
        Title = "",
        Subtitle = "Authentication Required",
        Note = "Get your key at: discord.gg/dkshub",
        FileName = "SHITTY KEY SYSTEM",
        SaveKey = false,
        GrabKeyFromSite = false,
        Key = {"NIGGERS"},
        Theme = customTheme
    }
})

local function tp(x, y, z)
    player:SetAttribute("State", 0)
    local char = player.Character or player.CharacterAdded:Wait()
    char:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(x, y, z)
end

local MainTab = Window:CreateTab("General", "box")
MainTab:CreateSection("AutoFarm")

_G.AutoClean = false

MainTab:CreateToggle({
   Name = "Mop Autofarm",
   CurrentValue = false,
   Flag = "AutoCleanToggle",
   Callback = function(Value)
      _G.AutoClean = Value
      if not Value then return end
      
      task.spawn(function()
         local RS = game:GetService("ReplicatedStorage")
         RS:WaitForChild("giveMop"):FireServer()

         while _G.AutoClean do
            local char = player.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not root or not hum then task.wait(1) continue end
            
            local mop = player.Backpack:FindFirstChild("Mop") or char:FindFirstChild("Mop")
            if mop and mop.Parent ~= char then hum:EquipTool(mop) end

            local folder = workspace:FindFirstChild("Cleaning_System") and workspace.Cleaning_System:FindFirstChild("Dirt_Spawn")
            if folder then
               for _, obj in pairs(folder:GetChildren()) do
                  if not _G.AutoClean then break end
                  
                  local part = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart")
                  if part then
                     tp(part.Position.X, part.Position.Y + 3, part.Position.Z)
                     task.wait(0.15)
                     
                     local prompt = obj:FindFirstChildWhichIsA("ProximityPrompt", true) or part:FindFirstChildWhichIsA("ProximityPrompt", true)
                     if prompt then
                        fireproximityprompt(prompt)
                     end
                     task.wait(0.25)
                  end
               end
            end
            task.wait(0.5)
         end
      end)
   end,
})

MainTab:CreateSection("Misc")

local AutoInflictEnabled = false

MainTab:CreateToggle({
   Name = "Kill All Players w/Weapon",
   Warning = true,
   CurrentValue = false,
   Callback = function(Value)
      AutoInflictEnabled = Value
      
      
      if AutoInflictEnabled then
         task.spawn(function()
            while AutoInflictEnabled do
               local gun = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
               
               if gun then
                  for _, player in pairs(Players:GetPlayers()) do
                     if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
                        local args = {
                           "Gun",
                           gun,
                           player.Character.Head,
                           {
                              {
                                 ChargeLevel = 0,
                                 ClientHitSize = Vector3.new(1.1705185174942017, 1.1870816946029663, 1.1705185174942017),
                                 Distance = 0.15243233740329742,
                                 ModuleName = "1",
                                 BulletId = "18,25|Bullet_{59d668ff-a!0 c - 4 2 0 9 - 8 5 6 1 - f e 1 8 7 4!+ 9 f c 9 }"
                              },
                              {
                                 ChargeLevel = false,
                                 ClientHitSize = false,
                                 Distance = false,
                                 ModuleName = false,
                                 BulletId = true
                              }
                           }
                        }
                        ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("InflictTarget"):FireServer(unpack(args))
                     end
                  end
               end
               task.wait(0.5) -- Adjust this delay to control how fast it fires
            end
         end)
      end
   end,
})

local CombatTab = Window:CreateTab("Combat", "bomb")
CombatTab:CreateSection("Silent Aim")

local SilentAimEnabled = false
local SilentTargetPart = "Head"

local Settings = {
    Enabled = false,
    Radius = 800,
    Color = Color3.fromRGB(255, 255, 255),
    Mode = "On Cursor",
    MaxDistance = math.huge
}

local SilentPart = nil
local hookInstalled = false

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true

local FOVCircle = Instance.new("Frame")
FOVCircle.Parent = ScreenGui
FOVCircle.AnchorPoint = Vector2.new(0.5, 0.5)
FOVCircle.Size = UDim2.fromOffset(Settings.Radius * 2, Settings.Radius * 2)
FOVCircle.BackgroundTransparency = 1
FOVCircle.Visible = false

local UIStroke = Instance.new("UIStroke")
UIStroke.Parent = FOVCircle
UIStroke.Color = Settings.Color
UIStroke.Thickness = 1

local UICorner = Instance.new("UICorner")
UICorner.Parent = FOVCircle
UICorner.CornerRadius = UDim.new(1, 0)

CombatTab:CreateToggle({
   Name = "Silent Aim",
   CurrentValue = SilentAimEnabled,
   Flag = "SilentEnabled",
   Callback = function(Value)
      SilentAimEnabled = Value
   end,
})

local function getMouseLocation()
    return UserInputService:GetMouseLocation()
end

local function getClosestPlayer()
    local Closest = nil
    local ShortestDistance = Settings.Radius
    local MousePos = getMouseLocation()
    SilentPart = nil
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local Humanoid = p.Character:FindFirstChildOfClass("Humanoid")
            local RootPart = p.Character:FindFirstChild("HumanoidRootPart")
            if Humanoid and Humanoid.Health > 0 and RootPart then
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    local dist = (LocalPlayer.Character.HumanoidRootPart.Position - RootPart.Position).Magnitude
                    if dist > Settings.MaxDistance then continue end
                end
                local success, ScreenPos, OnScreen = pcall(function()
                    return Camera:WorldToViewportPoint(RootPart.Position)
                end)
                if success and OnScreen then
                    local Magnitude = (Vector2.new(ScreenPos.X, ScreenPos.Y) - MousePos).Magnitude
                    if Magnitude < ShortestDistance then
                        local BoneObj = p.Character:FindFirstChild(SilentTargetPart) or RootPart
                        if BoneObj then
                            Closest = p
                            SilentPart = BoneObj
                            ShortestDistance = Magnitude
                        end
                    end
                end
            end
        end
    end
    return Closest
end

if not hookInstalled then
    hookInstalled = true
    local mt = getrawmetatable(game)
    local oldNamecall = mt.__namecall
    setreadonly(mt, false)
    mt.__namecall = newcclosure(function(self,...)
        local args = {...}
        local method = getnamecallmethod()
        if not checkcaller() and SilentAimEnabled and SilentPart and SilentPart.Parent then
            local targetHum = SilentPart.Parent:FindFirstChildOfClass("Humanoid")
            if targetHum and targetHum.Health > 0 then
                if method == 'FindPartOnRay' or method == 'FindPartOnRayWithIgnoreList' then
                    if args[1] and typeof(args[1]) == "Ray" then
                        local direction = (SilentPart.Position - args[1].Origin).Unit * 1000
                        args[1] = Ray.new(args[1].Origin, direction)
                    end
                elseif method == 'Raycast' then
                    if args[1] and typeof(args[1]) == "Vector3" and args[2] and typeof(args[2]) == "Vector3" then
                        if args[2].Magnitude > 500 then
                            local direction = (SilentPart.Position - args[1]).Unit * 1000
                            args[2] = direction
                        end
                    end
                end
            else
                SilentPart = nil
            end
        end
        return oldNamecall(self, unpack(args))
    end)
    setreadonly(mt, true)
end

CombatTab:CreateToggle({
    Name = "FOV Visible",
    CurrentValue = false,
    Flag = "FOVEnabled",
    Callback = function(Value)
        Settings.Enabled = Value
        FOVCircle.Visible = Value
    end,
})

CombatTab:CreateSection("FOV - Settings")

CombatTab:CreateDropdown({
    Name = "FOV Mode",
    Options = {"On Cursor", "Middle"},
    CurrentOption = {"On Cursor"},
    MultipleOptions = false,
    Flag = "FOVMode",
    Callback = function(Option)
        Settings.Mode = Option[1]
    end,
})

CombatTab:CreateSlider({
    Name = "FOV Radius",
    Range = {10, 800},
    Increment = 5,
    Suffix = "",
    CurrentValue = Settings.Radius,
    Flag = "FOVRadius",
    Callback = function(Value)
        Settings.Radius = Value
        FOVCircle.Size = UDim2.fromOffset(Value * 2, Value * 2)
    end,
})

CombatTab:CreateColorPicker({
    Name = "FOV Color",
    Color = Settings.Color,
    Flag = "FOVColor",
    Callback = function(Value)
        Settings.Color = Value
        UIStroke.Color = Value
    end,
})

RunService.RenderStepped:Connect(function()
    if Settings.Enabled then
        FOVCircle.Visible = true
        if Settings.Mode == "On Cursor" then
            local mousePos = UserInputService:GetMouseLocation()
            FOVCircle.Position = UDim2.fromOffset(mousePos.X, mousePos.Y)
        else
            FOVCircle.Position = UDim2.fromScale(0.5, 0.5)
        end
    else
        FOVCircle.Visible = false
    end
    if SilentAimEnabled then
        getClosestPlayer()
    else
        SilentPart = nil
    end
end)
